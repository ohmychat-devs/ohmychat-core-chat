import { supabase as db } from '@ohmychat/ohmychat-backend-core';

export default (token, callback) => {
    /*db.from('user_chats')
        .select('chat_id')
        .eq('user_id', token)
        .then(({ data, error }) => {
            callback(data);
        });*/
};